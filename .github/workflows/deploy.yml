name: Deploy AWS Portfolio

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout Code
        uses: actions/checkout@v3

      - name: ✅ Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: ✅ Make all scripts executable
        run: chmod +x infra/*.sh

      - name: ✅ Create S3 bucket + inject config.js + upload frontend
        run: ./infra/create-s3.sh

      - name: ✅ Create DynamoDB table (if not exists)
        run: |
          if ! aws dynamodb describe-table --table-name ContactMessages 2>/dev/null; then
            ./infra/create-dynamodb.sh
          else
            echo "✅ DynamoDB table already exists. Skipping."
          fi

      - name: ✅ Create IAM role (if not exists)
        run: |
          if ! aws iam get-role --role-name lambda-dynamodb-role 2>/dev/null; then
            ./infra/create-iam.sh
          else
            echo "✅ IAM role already exists. Skipping."
          fi

      - name: ✅ Deploy Lambda function
        run: ./infra/create-lambda.sh

      - name: ✅ Setup API Gateway
        run: ./infra/create-apigateway.sh

      - name: ✅ Inject API URL into config.js and re-upload to S3
        run: |
          echo "🔄 Updating frontend config.js with deployed API URL..."
          API_ID=$(aws apigateway get-rest-apis --query "items[?name=='ContactAPI'].id" --output text)
          API_URL="https://${API_ID}.execute-api.ap-south-1.amazonaws.com/prod/contact"
          echo "window.API_GATEWAY_URL = \"$API_URL\";" > frontend/config.js

          echo "📤 Re-uploading frontend to S3 with updated config.js..."
          BUCKET_NAME="siddhant-portfolio-2025"
          aws s3 sync ./frontend s3://$BUCKET_NAME/ --delete \
            --exact-timestamps \
            --cache-control "no-cache, no-store, must-revalidate"

      - name: ✅ Test Deployed API Endpoint
        run: |
          API_ID=$(aws apigateway get-rest-apis --query "items[?name=='ContactAPI'].id" --output text)
          URL="https://${API_ID}.execute-api.ap-south-1.amazonaws.com/prod/contact"
          echo "🌐 Testing API endpoint: $URL"
          curl -s -X POST "$URL" -H "Content-Type: application/json" \
            -d '{"name":"Test","email":"test@example.com","message":"Hello from GitHub Actions"}'